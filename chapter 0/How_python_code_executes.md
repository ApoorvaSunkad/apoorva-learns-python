1. Python code runs line by line (interpreter based)
2. No need to compile manually - **interpreter** handles it
3. Each line is converted into bytecode, then executed by the python virtual 
machine (PVM)
4. Errors stop execution immediately
5. Ex:
print('Hello')
print('world')
