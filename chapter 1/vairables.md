1. variables = name that stores a value in memory
2. In python, variables are used to store data that can be referenced and 
mainpulated during program execution
3. Ex:
x = 10
Y = 'Hello'
4. Python automtically decides the data type
5. Variables are references(labels) pointing to memory objects
6. Use id() to check memory location:
Ex: print(id(x))